import 'package:flutter/material.dart';

/// 검색 탭 화면 (책, 피드, 사람 검색)
class SearchTab extends StatelessWidget {
  const SearchTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('검색 탭 (책, 피드, 사람)'),
    );
  }
}